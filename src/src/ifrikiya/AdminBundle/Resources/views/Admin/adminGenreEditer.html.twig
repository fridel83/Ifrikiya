{% extends "::modelcatalogue.html.twig" %}

{% block title %}Administration Genre| editer{% endblock %}

{% block encodage %}
   {{ parent() }}
 {% endblock %} 
 
 
 
 
 
 {% block corps %}
 {% if is_granted('ROLE_ADMIN') %}
  <h1>Edition d'un Genre</h1>
<form action="{{ path('ifrikiya_admin_modifier_genre', { 'id': idgenre }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    
    <input type="submit" />
</form>
  {% endif %}
 {% endblock %}